Modification of the original ROS costmap_2d package: https://wiki.ros.org/costmap_2d

Dependencies for using openGL for costmap generation 
  sudo apt update
  sudo apt install libglm-dev 
  sudo apt install mesa-utils 
